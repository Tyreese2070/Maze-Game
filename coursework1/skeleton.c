#include <stdio.h>

// Maze game
struct Maze{
    int width;
    int height;
    char[50] filepath;
} maze;

// Move the user
void moveCharacter(char userInput, Maze*){
}

// Load maze file
void getMaze(){
}

// View an image of the map
void viewMap(){
}

// Main with command line args
int main(char argc, char *argv[])
{
    // Load the maze file

    // Validate maze file

    // Check for user input

    // Check for game events

    // Quit the game
}